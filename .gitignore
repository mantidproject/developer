# Ignore the generated site
_site/
# Ignore text editor (e.g. emacs) autosave files
*~
# don't lock the dependencies for bundler
Gemfile.lock
# don't pay any attention to code checkouts
mantid-code
mantid-systests
